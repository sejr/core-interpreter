program
  int N, A, B, C;

begin

  A = 0;
  B = 1;

  read N;

  while (N > 0) loop

    // Hello world!

    /*
     * This is a block comment
     * =======================
     * This can be used to provide a better explanation of some of the code you are working
     * on. None of this stuff will get past the tokenizer.
     */

    write A;

    C = A + B;
    A = B;
    B = C;

    N = N - 1;

  end;

end
